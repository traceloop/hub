apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "app.fullname" . }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "app.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "app.selectorLabels" . | nindent 8 }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- if not .Values.management.enabled }}
      # OSS mode - keep existing init container for config processing
      initContainers:
        - name: config-init
          image: busybox:latest
          command: ["sh", "-c"]
          args:
            - |
              cp /config-template/config.yaml /config/config.yaml
              # Dynamic substitution for all API keys
              {{- range $key, $value := .Values.secrets.keyMapping }}
              {{- $upperKey := upper (regexReplaceAll "ApiKey$" $key "") }}
              sed -i "s/{{ $upperKey }}_API_KEY_PLACEHOLDER/${{ $upperKey }}_API_KEY/g" /config/config.yaml
              {{- end }}
          env:
            {{- range $key, $value := .Values.secrets.keyMapping }}
            {{- $upperKey := upper (regexReplaceAll "ApiKey$" $key "") }}
            - name: {{ $upperKey }}_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ $.Values.secrets.existingSecretName }}
                  key: {{ $value }}
                  optional: {{ ne $key "openaiApiKey" }}
            {{- end }}
          volumeMounts:
            - name: config-template
              mountPath: /config-template
            - name: config-volume
              mountPath: /config
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.Version }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          workingDir: /app
          env:
            - name: PORT
              value: {{ .Values.service.port | quote }}
            {{- if .Values.management.enabled }}
            - name: MANAGEMENT_PORT
              value: {{ .Values.service.managementPort | quote }}
            - name: HUB_MODE
              value: "database"
            {{- if .Values.management.database.configMapName }}
            - name: POSTGRES_DATABASE_HOST
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.management.database.configMapName }}
                  key: POSTGRES_DATABASE_HOST
            - name: POSTGRES_DATABASE_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.management.database.configMapName }}
                  key: POSTGRES_DATABASE_PORT
            - name: POSTGRES_DATABASE_NAME
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.management.database.configMapName }}
                  key: POSTGRES_DATABASE_NAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.management.database.existingSecret }}
                  key: {{ .Values.management.database.passwordKey | default "password" }}
            - name: DATABASE_URL
              value: "postgresql://{{ .Values.management.database.user }}:$(DB_PASSWORD)@$(POSTGRES_DATABASE_HOST):$(POSTGRES_DATABASE_PORT)/$(POSTGRES_DATABASE_NAME)?sslmode={{ .Values.management.database.sslMode }}"
            {{- else }}
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.management.database.existingSecret | default (printf "%s-db-secret" (include "app.fullname" .)) }}
                  key: {{ .Values.management.database.passwordKey | default "password" }}
            - name: DATABASE_URL
              value: "postgresql://{{ .Values.management.database.user }}:$(DB_PASSWORD)@{{ .Values.management.database.host }}:{{ .Values.management.database.port }}/{{ .Values.management.database.name }}?sslmode={{ .Values.management.database.sslMode }}"
            {{- end }}
            - name: DB_POLL_INTERVAL_SECONDS
              value: {{ .Values.management.config.pollIntervalSeconds | quote }}
            # Dynamic environment variables from arbitrary secrets
            {{- range $envVar, $secretRef := .Values.management.secrets }}
            - name: {{ $envVar }}
              valueFrom:
                secretKeyRef:
                  name: {{ $secretRef.secretName }}
                  key: {{ $secretRef.key }}
                  optional: false
            {{- end }}
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
            {{- if .Values.management.enabled }}
            - name: management
              containerPort: {{ .Values.service.managementPort }}
              protocol: TCP
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds | default 30 }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds | default 10 }}
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds | default 5 }}
            successThreshold: {{ .Values.probes.liveness.successThreshold | default 1 }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold | default 3 }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds | default 10 }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds | default 10 }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds | default 5 }}
            successThreshold: {{ .Values.probes.readiness.successThreshold | default 1 }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold | default 3 }}
          {{- if .Values.management.enabled }}
          startupProbe:
            httpGet:
              path: /health
              port: management
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds | default 10 }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds | default 10 }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds | default 5 }}
            successThreshold: {{ .Values.probes.readiness.successThreshold | default 1 }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold | default 3 }}
          {{- end }}
          volumeMounts:
            {{- if not .Values.management.enabled }}
            # OSS mode - mount config file
            - name: config-volume
              mountPath: /app/config.yaml
              subPath: config.yaml
            {{- end }}
            # Management mode doesn't need config file volume
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        {{- if not .Values.management.enabled }}
        # OSS mode volumes
        - name: config-template
          configMap:
            name: {{ include "app.fullname" . }}-config
        - name: config-volume
          emptyDir: {}
        {{- end }}
        # Management mode doesn't need config volumes
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
